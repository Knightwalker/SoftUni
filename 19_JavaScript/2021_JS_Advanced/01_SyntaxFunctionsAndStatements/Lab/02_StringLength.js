function solve(param1, param2, param3) {
  let sum = Math.round(param1.length + param2.length + param3.length);
  let average = Math.round(sum / 3);
  
  console.log(sum);
  console.log(average);
}

// solve('chocolate', 'ice cream', 'cake');
// solve('pasta', '5', '22.3');